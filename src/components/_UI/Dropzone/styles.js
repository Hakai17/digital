import { Paper } from "@mui/material";
import { styled } from "@mui/system";

export const Container = styled(Paper)({
  display: "flex",
  alignItems: "center",
  justifyContent: "center",
  cursor: "pointer",
  // backgroundColor: isDragActive ? "#78E5D5" : "#F5F5F5",
});
