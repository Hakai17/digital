import { Grid } from "@mui/material";
import { styled } from "@mui/system";

export const Container = styled(Grid)({
  marginTop: 0,
});

export const FormFieldSection = styled(Grid)({
  width: "100%",
});
